# Belle Mobile App Environment Configuration
# Copy this file to .env.local and fill in the values

# API URLs
# For local development on simulator, use localhost
# For physical device testing, use your machine's IP address

# GraphQL Gateway (API Gateway)
API_URL=http://localhost:4000

# User Service (for file uploads)
USER_SERVICE_URL=http://localhost:3001

# WebSocket URL (Communication Service)
WS_URL=ws://localhost:3005

# App Variant (development, preview, production)
APP_VARIANT=development

# EAS Project ID (for Expo Application Services)
EAS_PROJECT_ID=your-eas-project-id
